# Viewer local configuration — copy this to .env and fill in your values.

# Cloud Run service URL (wss:// for WebSocket, https:// for token audience)
CLOUD_RUN_URL=https://your-cloudrun-service.run.app

# Shared auth token — must match AUTH_TOKEN set on the server
VITE_AUTH_TOKEN=your-secret-token
