# Binaries
cloudproxy-server
cloudproxy-pi-client

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib

# Node
node_modules/
dist/

# Placeholder embed target so `go build` works from server/ without a
# viewer build first; the root Dockerfile overwrites it with the real UI.
!server/viewer/dist/
!server/viewer/dist/index.html

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Local environment — contains secrets, never commit
.env
.env.local
.env.*.local
